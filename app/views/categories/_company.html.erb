<li class="list-group-item d-flex justify-content-between align-items-center" id="company_<%= company.id %>">
  <%= company.name %>
  <%= button_to 'Remove', remove_company_category_path(@category, company_id: company.id), method: :delete, data: { turbo_confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger' %>
</li>
