<div class="container">
  <h1>Edit Issue for <%= @company.name %></h1>
  <%= render 'form', company: @company, issue: @issue %>
  <%= link_to 'Back', company_path(@company, anchor: 'help-wanted'), class: 'btn btn-secondary mt-3' %>
</div>